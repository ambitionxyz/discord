import { Button } from "@mantine/core";

const ServerIdPage = () => {
  return (
    <div>
      <Button className="bg-red-500  text-blue-400">
        demo test css
      </Button>
    </div>
  );
};

export default ServerIdPage;
